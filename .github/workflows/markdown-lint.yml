name: Markdown Lint (Disabled)

on:
  push:
    branches: [ "main" ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Skip linting
        run: echo "Linting disabled - will re-enable later"
